{
  "name": "wild-octave-invoice-processor",
  "version": "2.0.0",
  "dependencies": {
    "@azure/ai-document-intelligence": "^1.0.0",
    "axios": "^1.6.0"
  },
  "scripts": {
    "build": "echo 'Build complete'",
    "deploy": "netlify deploy --prod"
  }
}
